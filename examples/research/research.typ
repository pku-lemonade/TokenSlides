#import "/lemonade.typ": *

#set text(lang: "zh")
#show: lemonade-theme.with(
    aspect-ratio: "16-9",
    box-compact: true,
    box-fill: true,
    // title-align: "left",
    // mode: "dark",
    config-info(
        title: [异构芯片与计算系统的设计与优化],
        author: [卓有为],
        date: none,
        institution: [北京大学],
        email: "youwei@pku.edu.cn",
        github: "pku-lemonade",
    ),
)

#title-slide()

= 芯片设计

== 在研：图计算加速器高层次综合

#ibox[
    *研究背景：* 图计算需求持续增长，面向图分析与图学习任务的专用加速器具有广阔应用前景。
]

#ebox[
    *核心挑战：* 现有设计流程效率偏低；不同优化框架难以组合复用；软硬件深度耦合，迭代成本高；验证工具不足，仿真速度慢。
]

#nbox[
    *研究内容：* 提出面向图计算加速器设计高层次描述语言 *Graph.HLS*，提出三级模块化设计抽象，并结合高效设计空间搜索与 IR 级快速仿真验证。
]

#sbox[
    *研究成果：* 相比手工优化设计，平均性能提升 2.6 倍；仿真速度提升 >300 倍；调试效率提升 >45 万倍，显著降低开发与维护成本。
]

== 在研：面向近存算的异构 Bank 架构设计

#ibox[
    *研究背景：* 近 Bank 计算可缓解内存墙问题，但现有架构在*每个* Bank 中集成计算逻辑，难以兼顾存储容量与计算能力。
]

#ebox[
    *核心挑战：* Bank 间数据移动受限，易形成新的内部通信瓶颈；热点访问分布不均，容易带来负载失衡与编程复杂性。
]

#nbox[
    *研究内容：* 提出异构近 Bank 架构 *vBank*，在同一 die 内协同计算 Banks 与容量 Banks，并构建基于 cluster 的统一抽象，结合计算迁移、动态聚类与虚拟化机制，实现高效数据访问与负载均衡。
]

#sbox[
    *研究成果：* 相比传统均匀式 NBP 基线获得 42.6%--51.3% 性能提升；在仅使用一半计算核心的情况下，平均加速 1.51 倍、能耗降低 43.8%，同时提供 50%--75% 更高的存储密度。
]

== 在研：面向大模型推理的异构 HBM-PIM 架构设计

#ibox[
    *研究背景：* 现有 HBM-PIM 在*每个*堆叠层都集成 PIM 逻辑，难以兼顾带宽需求与存储容量。
]

#ebox[
    *核心挑战：* 模型权重、历史 KV 与活跃 KV 的访问特征差异明显；跨层数据迁移依赖主机引入额外延迟。
]

#nbox[
    *研究内容：* 提出异构堆叠架构 *vStack*，将 HBM 容量层与 PIM 计算层分离，利用底层 die 上的控制器完成栈内 DMA、分层地址映射，并结合拓扑感知放置、工作负载感知淘汰与选择性复制策略。
]

#sbox[
    *研究成果：* 相比 AttAcc，平均归一化 p50 端到端时延降低 3.5 倍；PIM 层承担的 KV 服务比例由 18.3% 提升至 21.5%；容量层 KV 存储能力在不增加关键路径开销的情况下提升 2.667 倍。
]

== 在研：基于大语言模型的芯片设计自动化

#ibox[
    *研究背景：* 大语言模型在代码生成上的能力快速提升，面向 RTL 的自动化芯片设计展现出潜力。
]

#ebox[
    *核心挑战：* RTL 语法细节繁多、硬件语义强约束、模块间依赖复杂，导致大模型直接生成时生成成功率与测试通过率都较低。
]

#nbox[
    *研究内容：* 提出面向大语言模型的硬件 DSL MyGo 作为中间层，借鉴 Go 语法简洁、关键字少、结构显式、静态类型清晰、控制流自然等特点，更适合大模型稳定生成、增量修改与错误定位。
]

#sbox[
    *研究成果(预期)：* 显著提升代码生成成功率与功能测试通过率，减少了语法错误与低级实现缺陷，为大语言模型参与芯片设计提供了更可靠的中间表示与工具链支撑。
]


= 编译优化

== 规划：非同源多核 / 多芯粒的编译优化

#ibox[
    *研究背景：* 多芯粒集成已成为提升算力的重要路径，非同源异构集成快速发展，而编译工具链仍明显滞后于硬件演进，国产芯粒生态亟需系统性软件支撑。
]

#ebox[
    *核心挑战：* 计算、访存与通信高度耦合，传统性能模型难以适用；非同源边界约束复杂，缺乏有效的图切分与映射方法；多后端差异显著，代码生成效率不高。
]

#nbox[
    *研究内容：* 构建统一性能模型，研究全局任务映射与时空编排方法，优化跨芯粒数据布局与代码生成，并完成框架集成与多平台验证。
]

#sbox[
    *研究成果(预期)：* 形成多芯粒统一性能建模框架，构建跨芯粒协同编译工具链并开源，端到端推理吞吐率预期提升 30% 以上。
]

= 运行时系统

== 在研：多核 / 多芯粒性能可靠性理论与检测

#ibox[
    *研究背景：* 多核与众核加速器已得到广泛应用，但性能衰减类故障频发，片上实时检测与诊断的需求日益迫切。
]

#ebox[
    *核心挑战：* 片上存储资源极其有限，故障传播链条复杂，观测信号之间相互干扰，导致根因定位难度较高。
]

#nbox[
    *研究内容：* 设计负载感知的探针插桩机制，提出 Sketch 在线压缩结构，构建多层通信拓扑图模型，并提出 FailRank 根因定位算法。
]

#sbox[
    *研究成果：* 存储开销降低 116 倍，检测准确率达到 90%，并可扩展到不同众核架构。
]

== 在研：多智能体大模型推理系统优化（一）

#hbox[
    *研究重点：* 聚焦工作流调度与内存划分。
]

#ibox[
    *研究背景：* 多智能体应用日益普及，执行过程中需要频繁调用外部工具，推理与等待阶段交替出现。
]

#ebox[
    *核心挑战：* 工具调用会造成 KV cache 闲置，代理之间的缓存竞争又十分激烈，常规调度策略容易带来较高延迟。
]

#nbox[
    *研究内容：* TokenCake: 从时域上设计主动卸载与预取机制，从空域上进行动态内存划分与资源协调。
]

#sbox[
    *研究成果：* 相比 vLLM，端到端延迟降低 47% 以上，显存利用率提升 16.9%。
]

== 在研：多智能体大模型推理系统优化（二）

#hbox[
    *研究重点：* 聚焦批处理优化。
]

#ibox[
    *研究背景：* 多智能体模拟规模持续扩大，系统中呈现出典型的全收集式通信模式。
]

#ebox[
    *核心挑战：* 前缀错位会导致缓存复用失效，而全量存储又会带来较高的显存开销。
]

#nbox[
    *研究内容：* TokenGather: 设计面向批处理的混合缓存复用机制，采用主从差分存储以降低显存占用，并融合差分注意力机制。
]

#sbox[
    *研究成果：* 相比 vLLM，缓存存储空间降低约 60%，内存访存量降低约 65%。
]

#thank-you-slide(
    title: [感谢各位专家，敬请批评指正！],
)[
    #place-image(
        assets.qr-code,
        caption: "pku-lemonade",
        width: 20%,
        position: bottom + right,
        dx: 0em,
        dy: 1em,
    )
]
