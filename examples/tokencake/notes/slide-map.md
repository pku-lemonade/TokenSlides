# Tokencake Slide Map

Section divider slides will be generated automatically from level-1 headings in the Typst deck.

- `Title` | `Tokencake` | Takeaway: KV-cache management is the central systems bottleneck for multi-agent LLM serving. | Evidence: paper title + authors + venue metadata. | Archetype: title slide
- `Motivation` | `Agentic Workloads Stress KV Cache` | Takeaway: multi-agent DAGs with tool calls create long stalls and heterogeneous agent importance. | Evidence: Figure 1a/1b + Table 1 latency ranges. | Archetype: two-up comparison
- `Motivation` | `Space Contention` | Takeaway: FCFS-style memory allocation causes critical inversion when non-critical agents occupy scarce KV-cache blocks first. | Evidence: Figure 2a/2b. | Archetype: two-up comparison
- `Motivation` | `Time Underutilization` | Takeaway: tool stalls leave useful KV cache idle, and keeping it on GPU wastes memory that could admit active work. | Evidence: Figure 3a/3b, including the `18.5%` peak waste number. | Archetype: two-up comparison
- `Motivation` | `Why Existing Systems Miss It` | Takeaway: prior systems optimize either agent scheduling or KV-cache management, but not both at once. | Evidence: introduction comparison against Parrot, Autellix, Teola, vLLM, Mooncake, CachedAttention, LMCache; Table 2 trigger/policy contrast. | Archetype: table-led structured slide
- `Design` | `Tokencake Overview` | Takeaway: Tokencake co-optimizes space and time around the KV cache, not as a bolt-on scheduler tweak. | Evidence: Figure 4. | Archetype: method overview side-by-side
- `Design` | `Expose Agent Context to the Runtime` | Takeaway: the API surfaces DAG structure, internal function-call stages, and `predict_time`, which turns the scheduler into an application-aware controller. | Evidence: Figure 5 and Figure 6. | Archetype: two-up comparison
- `Design` | `Function-Call-Aware Time Scheduler` | Takeaway: offload only when a predicted stall is long enough to create useful scheduling capacity, then upload before resume. | Evidence: Figure 7 + Algorithm 1 logic + transfer-cost discussion. | Archetype: method overview side-by-side
- `Design` | `Dynamic Memory Partitioning` | Takeaway: Tokencake protects critical agents with adaptive reservations driven by hybrid priority and memory pressure. | Evidence: Figure 8 + static/dynamic priority formulas + Algorithm 2 summary. | Archetype: method overview side-by-side
- `Evaluation` | `Experimental Setup` | Takeaway: the evaluation stresses realistic multi-agent workloads on two Qwen sizes and compares against mainstream baselines. | Evidence: models, hardware, `100GB` CPU swap, Code-Writer, Deep Research, Poisson arrivals, vLLM, LightLLM. | Archetype: table-led structured slide
- `Evaluation` | `Latency Improves Under Load` | Takeaway: Tokencake's advantage is small at low QPS and large at high QPS, which matches the paper's memory-pressure thesis. | Evidence: Figure 9, especially the `47.06%` latency reduction vs vLLM at `1.0 QPS`. | Archetype: figure-led vertical
- `Evaluation` | `GPU Memory Stays Productive` | Takeaway: offloading stalled caches keeps utilization near `86-87%`, instead of leaving memory occupied but not useful. | Evidence: Figure 10 with the `16.9%` peak improvement over vLLM. | Archetype: figure-led vertical
- `Evaluation` | `Critical-Path Agents Stop Stalling` | Takeaway: agent-aware reservations cut pathological stalls, especially for latency-sensitive tool-heavy agents. | Evidence: Figure 12, including `File Write` abnormal agents dropping from `90` to `27` versus both baselines. | Archetype: figure-led vertical
- `Evaluation` | `Reuse Beats Recompute` | Takeaway: moving KV cache is cheap enough that offloading during long stalls is better than rebuilding the prefix later. | Evidence: Figure 13, including `4096` blocks: about `60 ms` transfer vs `8943 ms` recomputation. | Archetype: figure-led vertical
- `Evaluation` | `Mitigations Make Offloading Viable` | Takeaway: without CPU buffering and gradual reservation, upload latency explodes; with them, it stays in single-digit milliseconds. | Evidence: Figure 14, including `5120` blocks: `15163 ms` baseline upload vs `4.4 ms` optimized upload. | Archetype: figure-led vertical
- `Takeaways` | `What Matters in Tokencake` | Takeaway: the paper's contribution is the combination of function-call-aware time scheduling and criticality-aware memory partitioning, with a remaining limitation in single-GPU scope and prediction quality. | Evidence: synthesis of Figures 7, 8, 9, 10, 12, 14 and discussion section. | Archetype: summary boxes
